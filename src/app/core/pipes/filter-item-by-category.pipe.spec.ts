import { FilterItemByCategoryPipe } from './filter-item-by-category.pipe';

describe('FilterItemByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
